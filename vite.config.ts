import { defineConfig } from 'vite'

// Storybook configures the React plugin in .storybook/main.ts to avoid
// duplicate React Refresh injection. This file is kept for any future
// non-Storybook Vite usage.
export default defineConfig({})
